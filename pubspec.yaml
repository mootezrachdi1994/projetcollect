name: jamiyat_manager
description: تطبيق إدارة جمعيات مالية شهرية (Offline, RTL)
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0
  path: ^1.9.0
  path_provider: ^2.1.2
  drift: ^2.18.0
  drift_sqflite: ^2.0.1
  sqlite3_flutter_libs: ^0.5.23
  flutter_local_notifications: ^17.2.1
  timezone: ^0.9.4
  excel: ^4.0.6
  file_picker: ^8.1.2
  share_plus: ^10.0.2
  flutter_riverpod: ^2.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.8
  drift_dev: ^2.18.0

flutter:
  uses-material-design: true
  # Fonts can be added later when assets are available


